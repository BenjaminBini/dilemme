.container.well
	form.form-horizontal(name="profileForm")
		fieldset
			legend User profile
			.form-group
				label.col-md-2.control-label(for="username") Username
				.col-md-10
					input.form-control(name="username", type="text", placeholder="Username", ng-model="username", required)
			.form-group
				label.col-md-2.control-label(for="email") Email
				.col-md-10
					input.form-control(name="email", type="email", placeholder="Email", ng-model="email", required)
			.form-group
				label.col-md-2.control-label(for="password") Password
				.col-md-10
					input.form-control(name="password", type="password", placeholder="Password", ng-model="password")
			.form-group.actions
				.col-md-10.col-md-offset-2
					.pull-right
						button.btn.btn-primary(ng-click="update()", ng-disabled="profileForm.$invalid") Submit
						a.btn.btn-default(href="/") Back
.container
	.row
		.col-md-12
			table.table.table-hover
				thead
					tr
						th Your pending suggestions
						th Date
				tbody
					tr(ng-repeat="suggestion in suggestions")
						td
							{{suggestion.text}} : {{suggestion.answers[0].text}} or {{suggestion.answers[1].text}}
						td {{suggestion.published | date }}
	.row
		.col-md-12
			table.table.table-hover
				thead
					tr
						th Question
						th Upvotes
						th Answer 1
						th Answer 2
						th Publish Date
				tbody
					tr(ng-repeat="question in questions")
						td
							a(href="/admin/questions/{{question._id}}") {{question.text}} : {{question.answers[0].text}} or {{question.answers[1].text}} ?
						td {{question.upvotes}}
						td {{question.answers[0].votes}}
						td {{question.answers[1].votes}}
						td {{question.published | date }}