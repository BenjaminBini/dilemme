.col-md-5.answer(ng-click="sendAnswer()")
	span.answer-text {{answer.text}}
	.result
		i.glyphicon.glyphicon-ok(aria-hidden="true", ng-show="clicked") 
		span.percentage {{result}} % 
		span.votes ({{answer.votes}} votes)